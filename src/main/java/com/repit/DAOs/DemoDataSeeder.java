package com.repit.DAOs;

// DemoDataSeeder — DISABLED.
//
// This file used to seed a demo user (login: demo / demo123) with a profile,
// availability, and a full week of workout logs for screenshots / demos.
// It's commented out for normal app runs. To re-enable:
//   1. Restore the original implementation from git history (`git log -- src/main/java/com/repit/DAOs/DemoDataSeeder.java`)
//   2. Re-enable the import + seedDemoUser() call in Main.java
//
// Stub kept in place so the file compiles and so future devs can find the
// recipe via git blame instead of digging through history blind.
public class DemoDataSeeder {

    private DemoDataSeeder() { /* no instances — pure utility */ }

    /** No-op while disabled. See class header for how to re-enable. */
    public static void seedDemoUser() {
        // intentionally empty
    }
}
